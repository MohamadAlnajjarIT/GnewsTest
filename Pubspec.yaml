name: news_reader_app
description: A mobile application for reading news articles
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  http: ^1.1.0
  shimmer: ^3.0.0
  url_launcher: ^6.1.0
  cached_network_image: ^3.3.0
  intl: ^0.18.0
  pull_to_refresh: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
